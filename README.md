# Agent Watch — releases

Public appcast and release artifacts for **Agent Watch**. The source repository is private;
this repository exists because Sparkle cannot read a feed it has to authenticate for.

- `appcast.xml` — the Sparkle feed the app checks
- Releases — DMG/ZIP archives attached to GitHub releases

Nothing here is edited by hand: `mac-release` generates the feed and uploads the archives.
